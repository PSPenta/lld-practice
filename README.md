# Low Level Design (LLD) Practice Repository

This repository contains **machine-coding level solutions** to popular Low Level Design (LLD) problems.
Each folder contains code for a specific LLD problem implemented in **plain JavaScript**, following SOLID principles and clean design practices.

---

## 📚 What’s Included

Each sub-directory in this repository represents a standalone LLD project with working code and examples:

| Problem | Description |
|---------|-------------|
| **SplitWise** | A system to split bills among multiple users. |
| **LRU Cache** | Implementation of an LRU cache with TTL and eviction logic. |
| **ParkingLot** | Object-oriented parking lot design with floor & slot management. |
| **PollingSystem** | A poll creation and voting system. |
| **RateLimiter** | Multiple rate limiter strategies (TokenBucket, SlidingWindow, etc.). |
| **SearchEngine** | Simple search engine with tokenization, inverted index and ranking. |
| **UrlShortener** | A URL shortener with unique and (optionally) unpredictable keys. |
| **Database** | In-memory key-value Database with CRUD and basic indexing. |
| **Pub-Sub** | Publish-Subscribe system using EventEmitter and core JS logic. |
| **Queue** | Queue implementations showing basic enqueue/dequeue logic. |

---

## ▶ How to Use

1. **Clone the repository**
   ```bash
   git clone https://github.com/PSPenta/lld-practice.git
